project:
  name: quant-strategy-lab-crowding-baseline
  timezone: UTC

storage:
  root_dir: data/crowding-baseline
  raw_dir: data/crowding-baseline/raw
  normalized_dir: data/crowding-baseline/normalized
  features_dir: data/crowding-baseline/features
  reports_dir: reports/crowding-baseline

exchanges:
  - name: binance
    enabled: true
    market_types: [perp]
    quote_assets: [USDT]

research:
  bar_frequencies: [1h, 4h, 1d]
  benchmark_symbols: [BTC/USDT:USDT, ETH/USDT:USDT]
  universe_filters:
    min_avg_dollar_volume: 1000000
    exclude_stablecoins: true
