project:
  name: signal-lab-binance-recent3m
  timezone: UTC

storage:
  root_dir: data/binance-recent3m
  raw_dir: data/binance-recent3m/raw
  normalized_dir: data/binance-recent3m/normalized
  features_dir: data/binance-recent3m/features
  reports_dir: reports/binance-recent3m

exchanges:
  - name: binance
    enabled: true
    market_types: [perp]
    quote_assets: [USDT]

research:
  bar_frequencies: [1h, 4h, 1d]
  benchmark_symbols: [BTC/USDT:USDT, ETH/USDT:USDT]
  universe_filters:
    min_avg_dollar_volume: 1000000
    exclude_stablecoins: true
