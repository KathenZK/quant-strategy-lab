# Data Scripts

Scripts in this directory may mutate or extend the local data lake.

They should not contain strategy truth. Research logic belongs in generated one-off scripts or `docs/research/` summaries.
