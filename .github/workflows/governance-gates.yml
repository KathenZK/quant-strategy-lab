name: Governance gates

on:
  pull_request:
  push:

permissions:
  contents: read

jobs:
  governance:
    runs-on: ubuntu-22.04
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
      - uses: astral-sh/setup-uv@v6
        with:
          enable-cache: true
          cache-dependency-glob: uv.lock
      - name: Install locked test dependencies
        run: uv sync --locked --extra dev --extra ml
      - name: Run governance and data-contract preflight
        run: uv run --locked --extra dev --extra ml python scripts/governance/preflight.py
      - name: Run full test suite
        run: uv run --locked --extra dev --extra ml pytest -q
