# Archived Docs

This directory keeps historical planning documents that should not define the
active documentation flow.

- `mvp-implementation-plan.md`: early MVP implementation plan, retained as historical context.
- `canvas-migration-plan.md`: completed Cursor Canvas to Markdown migration plan.
- `legacy-canvas-migration-manifest.md`: completed mapping from legacy Cursor Canvas files to repository Markdown.
- `hype-cursor-artifacts/`: archived HYPE Cursor Canvas and agent artifact indexes after Markdown migration.

