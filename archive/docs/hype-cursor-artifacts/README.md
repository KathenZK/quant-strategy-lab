# HYPE Cursor Artifact Indexes

This directory archives the former repo-managed indexes for Cursor Canvas and
Cursor agent artifacts.

The Canvas content has been migrated to Markdown under `research/` and
the migration manifest lives at `../legacy-canvas-migration-manifest.md`.

These files are historical process evidence, not active research entrypoints.

