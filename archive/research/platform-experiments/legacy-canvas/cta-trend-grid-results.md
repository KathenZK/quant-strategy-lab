# CTA Trend Grid Results

> 迁移说明：本文由 legacy Cursor Canvas `cta-trend-grid-results.canvas.tsx` 自动转换为 Markdown；原 Canvas 未删除，仅作为历史来源。
> 归类：Platform / generic experiment Canvas。

Full-market 1h run over the locally available Binance spot universe: 25 symbols and 2,159 hourly bars.

### 关键指标

| 指标 | 数值 |
| --- | --- |
| Best net return |  |
| Best gross return |  |
| Best entries |  |
| Best avg turnover |  |

> **Main read**
> The rank gate was the main improvement: baseline net return moved from -8.37% to +0.90%, while entries dropped from 257 to 111. The best grid setting was max_positions=3, long_allocation=60%, confirmation=3, with net return +5.45%.

## Stepwise Checks

> 表格数据未能完全自动解析，请按源 Canvas 复核。

## Top Net Results

> 表格数据未能完全自动解析，请按源 Canvas 复核。

## All Parameter Groups

> 表格数据未能完全自动解析，请按源 Canvas 复核。

Baseline net was pct(baseline.net). Forward returns are average close-to-close returns after each entry at 1h, 4h, 12h, and 24h.

## 自动转换复核提示

以下数据数组包含 TypeScript 对象、表达式或 JSX，未必全部进入正文表格：

- `sortedRows`

以下组件存在降级转换提示，建议人工抽查源 Canvas：

- `table`
