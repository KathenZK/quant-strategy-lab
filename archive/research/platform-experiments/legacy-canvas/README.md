# Platform Experiment Canvas Reports

本目录由 Cursor legacy Canvas 批量迁移生成。Markdown 是后续维护入口；原 `.canvas.tsx` 文件仅作为历史来源。
冻结规则：本目录只保存从 Cursor Canvas 迁移来的历史证据，不作为新研究写入位置。新的结论应提升到对应 family 的 core ledger、`decision-log.md`、`canonical-specs/` 或 `diagnostics/`。


## 文件索引

- `cta-trend-grid-results.md`：源 Canvas `cta-trend-grid-results.canvas.tsx`；需人工抽查复杂组件。
- `spot-cta-diagnosis.md`：源 Canvas `spot-cta-diagnosis.canvas.tsx`。
- `spot-trend-param-scan.md`：源 Canvas `spot-trend-param-scan.canvas.tsx`。
