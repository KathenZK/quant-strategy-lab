experiment:
  name: rsi_window_sweep_example
  description: Sweep RSI windows from one base factor workflow and select the best Sharpe result.
  base_workflow: ../workflows/factors/factor_rsi.binance.btc.daily.recent1y.yaml
  max_workers: 2
  objective:
    metric: sharpe
    direction: max
  sweep:
    strategy.factor_name:
      - rsi_6
      - rsi_12
      - rsi_14
      - rsi_21
