# Retired Dashboard Root Package

This directory contains the retired root package wrapper for the archived
Dashboard frontend in `../web/`.

It is retained only for historical reference and is not an active development
entrypoint.

