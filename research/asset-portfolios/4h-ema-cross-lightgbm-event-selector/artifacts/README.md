# Artifacts — BIN-4H-EMAX-LGBM

保留的可复现证据：事件 parquet 与基线报告 JSON（含 4h 重采样衍生口径）。临时试跑输出放系统临时目录，不入本目录。

- `local_trend_selector/`：local+trend 选择器 OOF 分数与报告（V3 的信号源）。
- `v3_portfolio/`：V3 组合级回测报告、资金曲线（parquet/png）与三变体逐笔 parquet。
