# BIN-1D-TPSA-P0R：全资产输入范围修复

P0 运行后审计发现，它复用了旧的严格自然日连续块：只要相邻日期不是连续自然日就重新切段。这对 24/7 加密合约成立，但会把周末休市的 Binance 股票类永续合约反复切成短块，从而无法形成 60 个观测的前置状态。

P0R 只修复这一项：改用已经审计的 P3 日线缓存，并沿用其“相隔超过四个自然日才重开连续块”的观察序列。研究仍在 `2026-07-01` 前截止，至少上市 120 个自然日，至少需要 60 个前置观测。

以下内容全部不变：

- MA7/MA30 主要事件与 MA5/10/20 邻域；
- 所有前置状态只截止 `t-1`；
- 旧走势 × 最近走势的 `5 × 5` 状态矩阵；
- 十三种固定形态定义和阈值；
- 波动、效率、横盘状态；
- `1/3/5/10/20/40` 日结果；
- 决策树、LightGBM、目标和逐年前推窗口；
- 不做策略、不做账户、不报年化。

P0 已读结果不得用于改变任何分析条件。P0 标记为 `data-scope-incomplete`，最终报告以 P0R 为准。机器修复合同见 [`../configs/binance-1d-trend-prebreakout-state-atlas-p0r.json`](../configs/binance-1d-trend-prebreakout-state-atlas-p0r.json)。
