# CTLS首次绩效读取前修复附录

> 冻结时间：2026-08-10。该附录只处理首次Stage A严格落盘失败，不改变CTLS特征、状态阈值、搜索空间、窗口、交易规则、绩效门或LES访问顺序。

## 失败事实

- 原执行清单：`hype_1d_ma7_ctls_2026-08-10_manifest.json`，SHA256 `ae9e83a958e9b37844cacc31b58dcee8a1b8a9fababbdd97d0c2f326f507c53c`。
- 首次Stage A运行完成内存计算后，在严格JSON编码时因缺失类别指标为`NaN`而失败；目标Stage A文件尚未创建，没有部分文件、候选名单或绩效结果可供选择。
- 同次运行只输出了一条离线标签警告：最早尚未形成完整`SMA7`的中心窗口被尝试转换为MA侧别。该窗口应标为不可评估，而不是强制转换。
- Stage A不读取PnL；本次修复没有查看或使用任何Stage A排名、准确率数值、V4/V5/V6绩效或LES结果。

## 结果盲修复

1. 离线标签若中心7日所需`SMA7`存在非有限值，则该日标签保持缺失并从评估中剔除。
2. 内存排名仍使用`NaN`表达样本中不存在的类别；严格JSON证据层只把非有限浮点规范化为`null`，不得改写为0或参与排名。
3. 原manifest永久保留为失败链证据；修复后只能创建独占的`manifest_v2`和`stage_a_v2`，不得覆盖原文件。
4. 新增对应合成测试并重新锁定全部实现SHA；只有新preflight精确通过后才可再次运行Stage A。

