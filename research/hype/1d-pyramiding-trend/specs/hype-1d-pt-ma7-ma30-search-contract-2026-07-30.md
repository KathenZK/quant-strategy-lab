# HYPE-1D-Pyramiding-Trend MA7/MA30 搜索契约（2026-07-30）

## 身份与硬目标

- Family：`HYPE-1D-Pyramiding-Trend`（`HYPE-1D-PT`）内的 `MA7/MA30` 研究分支；不创建新家族，不继承 `HYPE-1D-MHEF` 或任何日内家族结论。
- 市场：Binance USD-M `HYPEUSDT` perpetual；信号周期为 UTC `1d`。
- 核心约束：趋势方向必须由固定的 `MA7/MA30` 相对关系或交叉确定；搜索不得把 `7/30` 偷换成其他均线长度。
- 最大下单目标杠杆：任一入场、加仓或调仓成交后的目标杠杆不得超过 `3.0x`；持仓期间因价格逆向波动导致的有效杠杆漂移另行报告，不把它伪装成新的主动加杠杆。
- 硬目标：年化权益倍数 `>20.0x`、保守最大回撤不超过 `20%`。胜率不设硬阈值，但必须按完整 campaign 报告。
- 证据下限：prefit 至少 `8` 个已平 campaign，锁定 holdout 至少 `3` 个；不足时只记为样本不足，不视为可信命中。

## 数据与冻结边界

- 原始输入：标准数据湖中已审计的 Binance `HYPEUSDT` perpetual `1h` raw/normalized K 线与实际 funding。
- 日线：只聚合恰好包含 `24` 根已收盘小时 K 的 UTC 自然日。
- 冻结时小时数据：`2025-05-30 10:00 UTC` 至 `2026-07-30 04:00 UTC`，共 `10,219` 根已收盘 K；数据质量 `blocker_count=0`。
- 当前完整日线预计范围：`2025-05-31 00:00 UTC` 至 `2026-07-29 00:00 UTC`；以 `2026-07-30 00:00 UTC` open 作为终点估值/执行价。
- prefit：从数据起点至 `2026-04-30 00:00 UTC` open，终点 exclusive；`2026-04-30` 为 embargo。
- 锁定 holdout：`2026-05-01 00:00 UTC` 至 `2026-07-30 00:00 UTC`，固定最后 `90` 天，flat-start。
- 参数搜索、消融和候选排序不得读取 holdout 收益、胜负、回撤或交易路径；冻结 shortlist 后只揭示一次。
- 研究限制：该 holdout 与 2026-07-22 的同家族研究区间大幅重叠，不能声称为研究者从未见过的纯净 OOS；报告必须标记为 `researcher-exposed locked holdout`，并用滚动窗口补充稳定性，但不得据此继续调参。

## 成本、成交与杠杆账本

- `t` 日只使用已收盘 OHLC 计算信号，基础成交在 `t+1` 日 open；额外审计 `K+2`。
- 每次实际成交按成交名义收取手续费 `0.001` 与不利滑点 `4 bps`；压力测试使用 `8 bps`。
- funding 按实际时间戳和上一时段真实持仓数量结算。
- 回测使用持仓数量、权益和成交名义账本；不允许把 `1x/2x/3x` 当成每日免费重置的收益乘数。
- 浮盈加仓测试两种冻结语义：
  - `layers`：初始杠杆后按盈利台阶增加目标杠杆，最高 `3x`；
  - `reset_to_3x`：达到盈利条件后一次增仓，把成交后的实际杠杆重置到 `3x`。
- 只有 campaign 未实现净盈利为正、方向仍满足 `MA7/MA30` 趋势且价格继续沿 `MA7` 运行时才允许加仓。
- 胜率按从首次建仓到完全平仓的 campaign 计算；加仓 fill 不拆成额外胜单。
- 回撤报告 open-to-open 与保守日内 high/low 界；若使用保护止损，止损价必须在前一已收盘日即可确定，跳空越过止损按下一可成交价并加滑点成交。

## 首要示例与边界

- 首要示例：`MA7` 上穿 `MA30` 后，只在 `close > MA7` 且 `MA7` 上行时做多；`MA7` 下穿 `MA30` 后，只在 `close < MA7` 且 `MA7` 下行时做空。
- 同值边界：`MA7 == MA30`、均线或 ATR 未完成 warmup、方向与价格确认冲突时保持 flat，不猜方向。
- 反转边界：同一收盘出现反向交叉时先生成平仓；若允许 flip，只能在下一 open 用一笔净成交完成，不允许同一日同时保留多空。
- 加仓与退出同日冲突：退出优先，加仓取消。
- flat-start holdout 不继承 prefit 仓位、pending order、峰值、冷却或加仓状态。

## 搜索范围与决策

- 先测试原始示例及明确边界，再在固定 `MA7/MA30` 核心下搜索：SMA/EMA、确认天数、cross-only/regime-follow/reclaim/breakout 入场、long/both/short、MA7/MA30 或 ATR/timeout 退出、保护止损、trailing、冷却、初始杠杆、加仓阈值与两种加仓语义。
- 只有同一冻结配置在 prefit、锁定 holdout flat-start 和 full 三个窗口均同时满足年化权益倍数 `>20x`、最大回撤 `<=20%`、交易数下限、实际杠杆上限与至少一次真实浮盈加仓，才算本轮有效命中。
- 未命中时保持 `explore / not promoted / not live-ready`；不得把短样本高年化、无交易窗口或全样本事后最优包装成合格策略。
