# Diagnostics

本目录保存数据质量、搜索结论、稳健性和 live-executable 审计报告。若 dry-run 前硬门槛没有同时通过，报告必须明确标为 `not promoted / not live-ready`，不得使用 candidate、dry-run、handoff、live 或 `NO-GO` 状态；`NO-GO` 只用于 dry-run/live 后的负面 forward 或实盘证据。

